# ============================================================
# WiTA v2 — requirements.txt
# Python >= 3.10 recommended
# ============================================================

# ── Core deep learning ──────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── VideoMAE backbone (HuggingFace) ─────────────────────────
transformers>=4.26.0

# ── Vision & data ───────────────────────────────────────────
Pillow>=9.0.0
numpy>=1.24.0

# ── String metrics ──────────────────────────────────────────
editdistance>=0.6.3

# ── HuggingFace dataset download ───────────────────────────
huggingface_hub>=0.20.0

# ── Korean support (optional — only needed for --lang korean/both) ──
# hgtk>=0.1.3          # uncomment if training on Korean data

# ── Notebook / experiment tooling (optional) ────────────────
# tqdm>=4.65.0         # progress bars in notebooks
# ipykernel            # Jupyter kernel for run_kaggle.ipynb
# tabulate>=0.9.0      # pretty-print tables in evaluator

# ── Dev / lint ───────────────────────────────────────────────
# pytest>=7.0
# ruff>=0.4.0
